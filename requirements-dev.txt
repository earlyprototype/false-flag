# Extras for content ingestion and terminal-graphics demos.
# Not required to play the game — install only if you work on
# ingestion/, Graphics/, or the cli/demos experiments.
-r requirements.txt
feedparser==6.0.11
prompt_toolkit>=3.0.0
terminaltexteffects==0.13.0
blessed==1.20.0
pytest
ruff
# dev-scripts/verify_shared_key.py drives the browser build headlessly.
# The package alone is not enough — it also needs a browser binary:
#   python -m playwright install chromium
playwright>=1.44
